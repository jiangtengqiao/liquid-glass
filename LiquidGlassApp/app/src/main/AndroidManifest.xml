<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 音乐播放器：后台播放 + 通知 + 本地音频访问 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 锁屏全屏播放器：Android 10+ 需要 FullScreenIntent 权限才能在锁屏上直接弹出 Activity -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 日历提醒：精确闹钟(Android 12+)，未授予时回退到非精确闹钟 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Android 11+ 包可见性：音乐播放器检测酷狗/汽水/QQ音乐是否安装需要 -->
    <queries>
        <package android:name="com.kugou.android" />
        <package android:name="com.luna.music" />
        <package android:name="com.tencent.qqmusic" />
        <package android:name="com.netease.cloudmusic" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".LiquidGlassApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiquidGlass"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 全屏锁屏播放器：专辑封面占满屏幕+多行歌词+手势滑动解锁 -->
        <activity
            android:name=".ui.LockScreenActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|screenLayout" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 音乐播放器后台服务：Media3 MediaSessionService，承担前台播放+自定义媒体通知+锁屏 -->
        <service
            android:name=".music.MusicService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- 媒体通知按钮事件接收器：RemoteViews 上一首/播放暂停/下一首/进度条拖动触发 -->
        <receiver
            android:name=".music.MediaButtonReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.liquidglass.app.MEDIA_PREV" />
                <action android:name="com.liquidglass.app.MEDIA_NEXT" />
                <action android:name="com.liquidglass.app.MEDIA_PLAY_PAUSE" />
                <action android:name="com.liquidglass.app.MEDIA_SEEK" />
            </intent-filter>
        </receiver>

        <!-- 日历提醒接收器：接收 AlarmManager 到点广播并弹通知 -->
        <receiver
            android:name=".ui.ReminderReceiver"
            android:exported="false" />
    </application>
</manifest>